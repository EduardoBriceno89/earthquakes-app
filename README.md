# README EARTHQUAKES APP

* Ruby version: 3.3.0
* Rails version: 7.1.3
* DB: PostgreSQL

Para correr la task:

```bash
rake usgs_data:fetch
```

para levantar el servidor del cliente (React) ingresar a la carpeta "client" e ingresar el comando:

```bash
npm run dev
```